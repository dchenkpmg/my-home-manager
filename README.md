# My Home Manager

Personal Nix Home Manager config.

## Setup

1. Install Nix: <https://nixos.org/download/>
2. Clone this repo to `~/my-home-manager`
3. `make install`

## Commands

```
make switch   # apply config
make update   # update flake inputs
make clean    # gc nix store
```
